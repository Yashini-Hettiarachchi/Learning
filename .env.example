
# Required: MongoDB connection string. Example for MongoDB Atlas:
MONGODB_URI=mongodb+srv://<username>:<password>@cluster0.example.mongodb.net/mydatabase?retryWrites=true&w=majority

# Optional: JWT secret. If not provided, the server will derive a secret from the MongoDB URI.
# For production you may set a dedicated JWT_SECRET for extra control.
# JWT_SECRET=replace_this_with_a_long_random_secret

# Copy this file to .env and set real values. Do NOT commit real credentials.
